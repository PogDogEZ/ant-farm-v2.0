apply plugin: "java"

repositories {
    mavenCentral()
}

dependencies {
    implementation group: "org.json", name: "json", version: "20220320"
    // implementation group: "black.ninia", name: "jep", version: "4.0.3"
}

jar {
    baseName = "yescom"
    classifier = "api"
}
